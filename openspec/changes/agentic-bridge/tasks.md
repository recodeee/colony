# Tasks

## Phase 1: Reflexion Wave

- [ ] Define reflexion capture contracts for self-critique, decision review,
  and follow-up materialization.
- [ ] Add validation coverage for reflexion spec deltas before behavior work.

## Phase 2: Retry Wave

- [ ] Define retry contracts for bounded attempts, failure classification,
  backoff, and durable retry evidence.
- [ ] Add validation coverage for retry spec deltas before behavior work.

## Phase 3: ONNX Wave

- [ ] Define ONNX contracts for local runtime boundaries, packaging constraints,
  and optional integration points.
- [ ] Add validation coverage for ONNX spec deltas before behavior work.

## Phase 4: Lineage Wave

- [ ] Define lineage contracts that trace agentic outputs to prompts, artifacts,
  runtime choices, retry decisions, and reflexion records.
- [ ] Add validation coverage for lineage spec deltas before behavior work.

## Completion And Cleanup

- [ ] Validate with `openspec validate --specs`.
- [ ] Record PR URL, merge state, and sandbox cleanup evidence when finished.
