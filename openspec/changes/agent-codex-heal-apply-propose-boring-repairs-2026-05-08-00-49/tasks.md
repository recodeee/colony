# Tasks

- [x] Inspect existing health fix-plan, coordination sweep, claim, and search behavior.
- [x] Add `colony heal` dry-run proposal output.
- [x] Add `colony heal --apply` action-by-action approval with `--yes` for automation.
- [x] Implement expired quota-pending claim release as a repair action.
- [x] Implement protected-branch claim redirect to one existing matching `agent/*` task.
- [x] Record every applied action as a searchable `repair` observation.
- [x] Add focused CLI tests and command registration coverage.
- [x] Run focused tests, touched-file Biome check, CLI typecheck, no-bridge check, build, and OpenSpec validation.

## Cleanup

- [ ] Finish PR, merge, and sandbox cleanup; record PR URL and `MERGED` evidence.
